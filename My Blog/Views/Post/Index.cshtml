
@{
    ViewBag.Title = "文章列表";
}

<h2>文章列表</h2>

<ul>
    @foreach (var post in Model.Posts) {
        <li>
            @Html.ActionLink((string)post.Title,"Get",new {id=post.Id})
        </li>
    }
</ul>

